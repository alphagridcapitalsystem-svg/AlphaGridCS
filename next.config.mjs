/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  allowedDevOrigins: [
    "jab-certainly-wildcard.ngrok-free.dev",
  ],
}

export default nextConfig
